# ROADMAP.md — 다음 작업 백로그 (2026-07 기준)

> 새 세션이 이 문서만 읽고 바로 작업을 고를 수 있도록 우선순위·근거·시작점을 적는다.
> 항목을 시작하기 전에 **RECIPES.md** 에서 해당 레시피(건물/적/작업/자원/테스트)를 먼저 읽을 것.
> 완료한 항목은 체크(`[x]`)하고 커밋 해시를 남긴다. 새로 생긴 아이디어는 알맞은 티어에 추가.

---

## 티어 0 — 바로 처리(가벼움, 세션 워밍업용)

- [x] **최근 콘텐츠 도전과제(GOALS) 반영**(dc6ce2b) — `js/goals.js`(19번째 줄 `GOALS` 배열). 성문·찬란한 음식·초특급 희귀 낚시 스팟이 추가됐는데 도전과제엔 없음. 예: "출입 통제"(성문 1개 완공) · "기적의 만찬"(찬란한 음식 1회 섭취) · "심해의 전설"(spotOnly 어종 1회 포획). `test` 함수는 `world`/`pawns` 상태만 보므로 플래그가 없으면 `world.stock`/`world.buildings` 직접 검사로 판정(예: `countBuilt(w,'fenceGate')>=1`). 완료 시 sim-smoke 시나리오 1개 추가(레시피 5-A).
- [x] **hook-style.mjs 활성화**(87d1c60) — `.claude/settings.json` 에 PostToolUse(Edit|Write) 훅으로 등록 완료(대장님 승인). `js/*.js` 편집 시 `check-style` 자동 실행 + 위반 시 exit 2 로 모델에 피드백. 라이브 발동까지 확인.
- [x] **ARCHITECTURE.md 정기 드리프트 점검**(abbd865) — 이번 세션 변경(미니악마·가금부화·deliver·부상·장식 직접건설) 반영: 재고 키·enemyCbs·예약락 네임스페이스(`item:` 제거·`tame:` 추가)·적 kind(`'invwarrior'`→`'warrior'`, warlord/minidemon 추가) 정정. **앞으로도 기능 추가마다 관련 섹션 한 줄이라도 대조하는 습관 유지.**
- [x] **test:invariants 소크에 건설(blueprint) 배치 커버리지 추가**(8734f11) — 소크가 하루 한 번 decoLog 설계도를 배치해 deliver→build 파이프라인을 30일 내내 반복(시드당 11~19채 완공). 완공 0채면 실패 처리하는 회귀 가드 포함.

## 티어 1 — 레시피 그대로 적용 가능(1세션 완결, 설계 불필요)

- [x] **연구(RESEARCH) 트리 확장**(7382e68) — 관개·수의학·요새화 3종 추가(항해술은 보류).
- [x] **작물·요리 재료 다양화**(ab5e3ad) — 간단 버전으로 확정(대장님 확인): 밀 농사는 그대로 두고 채집 전용 신규 재료 「당근」+COOK_TIERS 신규 등급 「채소죽」 추가.
- [x] **가축 사육 산출물 다양화**(7382e68) — 양이 양털(`wool`)도 겸용 산출.
- [x] **유틸리티 건물 보강**(7382e68) — 정자(사기 증진) 추가. 저장고 특화(부패 방지)는 애초에 decay 시스템이 없어 제외(대장님 확인).
- [x] **배(이동수단)**(e48f668, 로드맵 외·대장님 요청) — 재화로 건조(목재 25+철 3)해 정착민이 타고 물 위를 항해(뗏목 다리 없이 바다 건너 대륙·섬으로). 직접 조종(WASD)·isWalkable 재활용. **1인승 MVP** — 후속: 다인승·화물 운반은 미구현(별도 세션).
- [x] **골드 소비 건물 3종**(b3c9ae4, 로드맵 외·대장님 요청) — 다른 게임 벤치마킹: 장비 상점(Stronghold 무기고, 대장간 없이 금으로 무기·방어구 즉시 구매)·도서관(Civilization, 연구 속도 +25%/채 중첩)·여관(Banished/Frostpunk, 금+식량으로 축제 개최 → 전체 사기 상승, 쿨다운 3일). 도전과제 3개 + sim-smoke 시나리오 2개 추가.

## 티어 2 — 설계 먼저, 2~3세션 규모

- [x] **겨울(SEASONS) 실제 위협화**(3157d11) — 냉기(`pawn.cold`) + 온기 반경(모닥불 3.5·전용 난로 7) + 난로 장작 소비(겨울) + 겨울 예고 토스트 + 🥶 체온 UI 바. 허기 시스템과 대칭 설계. 밸런스값은 `config.WINTER` 에 집약.
- [x] **정착민 부상 시스템**(67fd621) — `pawn.injury`(다리=이동↓·팔=작업↓) + 치료소 완치(자연 치유 없음). 전투 피격 3곳(근접·거인점프·데몬레이저)에 확률 부여, `updateEnemies` 에 rng 인자 추가.
- [x] **도전과제(GOALS) 전면 확장 + 진행률 UI**(67fd621) — 11→21개로 확장(원정섬 전부 발견·보스 처치·연구 올클리어·유물 수집 등) + 목표 모달에 "달성 N/전체" 표시.

## 티어 3 — 대형 설계 필요(리서치→설계 문서→구현, 여러 세션)

- [ ] **아보랑카도 신앙 시스템 심화** — 이전 세션에서 검토만 하고 미착수. 현재 「아보랑카도」는 `GODDESS.day`(7일차) 밤 1회성 강림 후 끝(`js/config.js` GODDESS, `js/sim.js` 강림 로직). 확장 아이디어: 제단 건물 신설 → 정착민이 주기적으로 "기도"(신규 job 타입, 레시피 3 그대로 적용 가능) → 신앙도(favor) 자원처럼 축적 → 임계값마다 자동/선택적 축복(RELICS 재사용). RimWorld ideology, Frostpunk faith 노선 벤치마킹. **먼저 짧은 설계 메모(원하는 루프·리스크)를 만들고 착수 권장** — 세이브 스키마 영향 있으면 `save.js` 버전 업 필요.
- [ ] **정착민 사회/관계 시스템** — 정착민 간 호감도·갈등(다툼·불화로 사기 저하 등). 완전 신규 시스템(관계 매트릭스 자료구조, 이벤트, UI 패널)이라 스코프가 큼. 소규모 파일럿(정착민 쌍마다 호감도 하나만) → 반응 좋으면 확장 추천.
- [x] **운반(haul) 자동화를 건설까지 확장**(d31f071) — 가벼운 버전(가상 창고 왕복)으로 확정(대장님 확인): 실제로는 이미 완전히 구현돼 있었으나 한 번도 실행되지 않던 `deliver` 잡 파이프라인을 활성화. 바닥 물리 적재(`world.items`)까지 갈아엎는 진짜 무거운 버전은 별도 세션 대상으로 계속 미룸.

---

## 진행 원칙 (모든 티어 공통)
1. 항목 착수 전 RECIPES.md 해당 레시피 Read → 체크리스트로 사용.
2. `npm run check:style` → 구현 → `npm run test:sim` → `npm run test:invariants` → (새 기능이면) sim-smoke 시나리오 추가.
3. 세이브 스키마를 바꾸면 `js/save.js` 의 `v` 를 올릴지 판단(필드 추가뿐이면 대부분 불필요 — ARCHITECTURE.md 세이브 섹션 참고).
4. 완료 후 이 문서에 체크 표시 + 커밋 해시 기록, 새로 떠오른 아이디어는 알맞은 티어에 추가.
