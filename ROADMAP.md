# ROADMAP.md — 다음 작업 백로그 (2026-07-06 전면 재설계)

> 새 세션이 이 문서만 읽고 바로 작업을 고를 수 있도록 우선순위·근거·시작점을 적는다.
> 항목을 시작하기 전에 **RECIPES.md** 에서 해당 레시피(건물/적/작업/자원/테스트)를 먼저 읽을 것.
> 완료한 항목은 체크(`[x]`)하고 커밋 해시를 남긴다. 새로 생긴 아이디어는 알맞은 티어에 추가.
>
> **재설계 배경**: 이전 로드맵의 미완 항목을 그대로 잇지 않고 게임 전체를 재진단해 우선순위를
> 다시 세웠다(승계한 항목은 「승계」 표기). 완료 기록은 하단 「완료 히스토리」로 압축 보관.

---

## 진단 — 무엇이 비어 있는가

**이미 잘 갖춘 것**: 완결된 진행 아크(무리→나라 랭크 5단계, 습격→괴민→대침공→최종보스→탈출선 승리),
로그라이트 유물 11종, 신앙 Phase 1, 낚시(어종 테이블·낚싯대·희귀 스팟), 원정 섬 4, 겨울 냉기,
부상, 역할/스킬/트레잇, 골드 싱크 3종(장비상점·도서관·여관), 떠돌이 상인(매입).

**구조적 공백 4가지** (콘텐츠 "양"이 아니라 구조의 문제):

1. **모든 사건이 고정 시간표.** 습격 3일 간격·괴민 5일·상인 4일·여신 7일·대침공 10/20일 —
   무작위 사건이 문자 그대로 0개. 2회차부터 게임이 "일정표 관리"가 된다.
   림월드형 장르의 심장(예측 불가능한 이야기 생성)이 비어 있다. → **최우선 보강 축**.
2. **경제가 단방향.** 상인(`config.TRADER`)이 자원을 사기만 하고 팔지 않는다.
   금 싱크는 생겼지만(장비상점 등) 금으로 "기회"를 사는 경로가 없어 순환이 반쪽.
3. **정착민이 무언극 배우.** 트레잇·스킬이 숫자로만 존재하고 개성이 드러나는 순간이 없다.
   사망해도 토스트 1줄 — 애착이 생길 장치가 없다.
4. **승리가 싱거움.** 탈출선(자원 ~2,600 메가프로젝트) 완성 보상이 토스트 1개(`sim.js` 270행 부근).
   에필로그·기록·탈출 후 선택이 없다.

**설계 축 우선순위**: 예측불가능성(사건) > 경제 순환 > 정착민 서사 > 승리·재플레이 > QoL > 그래픽 마감.

---

## 티어 0 — 바로 처리(가벼움, 세션 워밍업용)

- [ ] **정착민 사망 여파** — 지금은 죽으면 토스트 1줄뿐. ①전체 사기 일시 하락(`pawns.js` 기분 수렴식
  `moodTarget` 에 며칠짜리 감산 항 — `world.mourningUntil` 게임분 기록) ②이벤트 로그에 남긴 기록
  (생전 최고 스킬·생존 일수) ③(선택) 무덤 장식물 자동 배치. 죽음에 무게를 주는 최소 장치.
  시작점: `pawns.js` `updatePawn` 사망 분기 2곳(굶주림·동사)과 `enemyCbs.onPawnDeath`(main.js).
  수용: sim-smoke — 사망 후 생존자 mood 목표치가 일시 하락했다가 회복.
- [ ] **세이브 슬롯 3개** — 현재 `tinyColony.save1` 단일 키(`save.js`). 키를 `save1~save3` 으로
  파라미터화 + 시작 화면/저장 버튼에 슬롯 선택(각 슬롯에 일차·인구·랭크 미리보기). 스키마 무변경(v15 유지).
- [ ] **콜로니 달력 패널** — 다음 습격 D-day 경고는 이미 있음(`ui.js updateInvasionWarning`).
  상인 방문·괴민·대침공·여신까지 한 패널에 통합해 "다가올 일" 을 한눈에. 순수 UI(시뮬 무접촉).
  ※ 티어 2 사건 시스템이 들어오면 이 달력에 "?" 슬롯(예고 없는 사건)이 생겨 대비가 된다.
- [ ] **새 게임 난이도 3단계** — 평화/보통/도전. `RAID`/`GIANT_RAID`/`INVASION` 수치에 배율 하나
  (`world.difficulty` 저장, 스폰 수 계산부에서 곱함). 소크 불변식은 보통 기준 유지.
  근거: 무개입 30일 소크가 전멸이 기본일 만큼 압력이 있는 게임 — 신규 유입용 완충이 없다.

## 티어 1 — 1세션 완결(레시피 적용 가능)

- [ ] **상인 양방향 교역(구매 목록)** — 경제 순환 완성의 반쪽. `TRADER.sells` 테이블 신설:
  기본 자원 역구매(목재·철·식량, 매입가의 2~2.5배) + 프리미엄 품목(무작위 일반 유물 1개 고가 ·
  가축 1마리 · 찬란한 음식 1개 — 방문마다 ambient rng 로 1~2종 로테이션). 상인 UI 에 구매 탭.
  시작점: `config.TRADER` · 상인 UI(`ui.js`) · 매입 처리부(main.js `trader` 검색).
  결정론: 로테이션 선택은 `stepWorld` 의 rng 경유(레시피 준수). 수용: sim-smoke — 금 차감·품목 지급.
- [ ] **정착민 한마디(말풍선)** — 서사 장치 최소 구현. 상황 트리거(허기 위험·겨울 추위·전투 승리·
  축제·유물 획득·작업 완료 가끔)에 트레잇별 톤의 짧은 대사를 머리 위 말풍선으로 2~3초.
  **렌더 전용**(대사 선택은 render.js — 시뮬 rng 무접촉이라 결정론 무영향). ctx 신규 콜백 불필요:
  기존 `onEvent`/상태 관찰로 렌더가 스스로 판단. 시작점: `render.js updatePawnSprite`(이미 pawn 상태 접근).
  수용: 브라우저 확인 + check:style(렌더는 Math.random 허용 파일인지 확인 — 아니면 시드 파생).
- [x] **사건 파일럿 3종**(ceb4721) — 「유랑 행상」(매입가 1.5배 + 상인 없으면 하루 방문 소환) ·
  「늑대 이동철」(2일간 맹수 선공, hp 하한 클램프로 즉사 금지) · 「풍요의 바람」(1일 작물 성장 2배).
  `config.EVENTS` + `rollDailyEvent`/`tradeRate`(world.js) + sim.js 일 넘김 훅 + `world.activeEvent`
  세이브(v15 유지). 30일 발생 빈도 5~9회(3~6일 1회꼴) 실측. sim-smoke 58(12단언) + 소크 통과.
  **잔여 관찰**: 시드별 종류 편중(같은 사건 연속) — 티어 2 스토리텔러의 가중 추첨에서 개선.
- [ ] **그래픽 마감(승계·미승인)** — ①성(castle) 대체 탐색(+`def.town` 렌더 우선순위 의문점 실물 확인:
  이전 로드맵 티어1 참고) ②겨울 눈 타일(Sunnyside) ③장식물 교체(Kenney 32px) ④야생동물 교체
  (MiniWorld/Sunnyside). 에셋 선정 프로토콜(메모리) 준수 — 라이선스·실물검수 먼저.

## 티어 2 — 설계 먼저(2~3세션 규모)

- [ ] **사건 시스템 본격화(스토리텔러 라이트)** — **설계 완료 → [DESIGN-STORYTELLER.md](./DESIGN-STORYTELLER.md)**
  (체급 S·통증 P 2축 긴장도, 가중 추첨+쿨다운, 신규 사건 6종, 구현 웨이브 6개 — 구현 세션은 설계서부터 Read).
  파일럿 골격 위에 긴장도 기반 스케줄러.
  `tension = f(자원총량, 인구, 최근 피해)` 를 매일 계산 → 사건 풀에서 강도·긍/부정 가중 추첨.
  부정 사건 후보: 전염병(정착민 며칠 작업↓)·곡물 병충해(밭 일부 시듦)·도적 잠입(재고 소량 도난).
  긍정 사건 후보: 유랑단 합류 지원(고용비 반값)·철 노두 발견(임시 철광)·풍어기(낚시 확률↑).
  원칙: **사건은 판을 흔들되 즉사시키지 않는다**(최대 피해 상한을 config 에 명시).
  시작점: `config.EVENTS` 확장 + `sim.js` 일 넘김 훅. 수용: 소크 불변식 6시드×30일 통과(전멸 유발 금지)
  + 사건별 sim-smoke.
- [ ] **관계 파일럿(호감도 1축)** — 이전 로드맵 티어 3 「사회/관계 시스템」의 축소 선행판.
  정착민 쌍마다 호감도 하나(`world.relations` 맵, `"idA:idB"` 키). 같은 작업 카테고리 동시 수행·
  축제 참여 시 서서히 ↑, 사건(다툼)으로 ↓. 임계값에 절친(같이 있으면 사기↑)/앙숙(사기↓) 2상태만.
  UI 는 정착민 패널 한 줄("💚 단비와 절친"). 반응 좋으면 티어 3 확장, 아니면 여기서 멈춤(매몰비용 최소).
- [ ] **승리 시퀀스 강화** — 탈출선 완성 시: ①정착민들이 배로 걸어가 탑승하는 연출(기존 boat/침공
  상륙 연출 재사용) ②에필로그 모달(생존 일수·사망자 명단·처치 수·달성 목표 N/24·획득 유물)
  ③"떠난다(엔딩·기록 저장)" vs "남는다(프리플레이 계속)" 선택. `world.escaped` 플래그 이미 존재(`world.js` 176행).
  시작점: `sim.js` `shipComplete` 분기 → ctx 신규 콜백 `onVictory(stats)`(ARCHITECTURE ctx 목록 갱신 필수).
- [ ] **신앙 Phase 2(승계)** — 능동 기도 job(레시피 3) → 신앙도 가속 + 축복 **선택 메뉴**(favor 소비)
  + 축복 2~3종(풍요·가호·치유의 기적) + 도전과제 1개. 이전 로드맵 정의 그대로 유효.

## 티어 3 — 대형(리서치→설계 문서→구현, 여러 세션)

- [ ] **2번대륙 테마 콘텐츠 「잊힌 문명의 폐허」** — 현재 2번대륙은 지형·광산만 있는 빈 확장지.
  폐허 유적 3~5곳(탐사 지정 → 발굴 작업 → 유물 조각/설계도/골드) + 수호 골렘(신규 적 1종) +
  발굴 전용 도전과제. 원정 섬(1회성 보상)과 달리 **반복 채굴형 중반 목적지**로 차별화.
  ※ `createWorld` rng 소비 순서 주의(메모리·ARCHITECTURE) — 파생 시드(`mulberry32(seed ^ 상수)`) 사용.
- [ ] **스토리텔러 성격 3종** — 티어 2 사건 스케줄러에 프로파일(온화: 긍정 가중·부정 완충 /
  표준 / 잔혹: 연쇄 사건 허용). 티어 0 난이도 옵션과 새 게임 화면에서 통합.
- [ ] **뉴게임+(로그라이트 계승)** — 탈출 엔딩 시 유물 1개를 다음 회차로 계승 + 회차 카운트에 따라
  적 강화. 유물 시스템(RELICS)이 이미 스택 설계라 계승 훅만 필요. 재플레이 가치의 핵심 장치.
- [ ] **사회 시스템 확장(조건부)** — 관계 파일럿 반응이 좋을 때만: 다툼 이벤트·중재·연인/결혼 등.
- [ ] **신앙 Phase 3(승계)** — 분노(favor 방치·사망 페널티) + 신앙 게이트 콘텐츠 + 여신 재강림.

---

## 보류·제외 판단 (근거 포함)

- **작업 우선순위 매트릭스(림월드식)** — 역할 7종이 이미 그 역할. 오늘(543fca2) 역할·양보 상호작용
  버그를 고친 직후라, 제어 축을 하나 더 얹기보다 역할 시스템 안정화가 먼저. 필요 신호(유저 불편)가
  생기면 "정착민별 작업종류 금지 토글" 최소판부터.
- **바닥 물리 적재(world.items 실사용) 운반 개편** — 가상 창고 모델로 현재 문제없음. 전면 개편은
  버그 표면적만 넓힘(이전 로드맵에서도 반복 보류). 명확한 게임플레이 이득이 정의되기 전까지 제외.
- **다인승·화물 배** — 배 1인승 MVP 사용 빈도부터 관찰. 2번대륙 콘텐츠(티어 3)가 들어오면
  화물 수요가 생기므로 그때 재평가.

## 완료 히스토리 (이전 로드맵에서 압축 이관)

<details>
<summary>2026-07 완료분 (펼치기)</summary>

- 티어 0: GOALS 최근 콘텐츠 반영(dc6ce2b) · hook-style 활성화(87d1c60) · ARCHITECTURE 드리프트 점검(abbd865) · 소크 건설 커버리지(8734f11)
- 티어 1: 연구 3종 확장(7382e68) · 당근+채소죽(ab5e3ad) · 양털(7382e68) · 정자(7382e68) · 배 MVP(e48f668) · 하단 툴바 개편(a4752af) · 그래픽 이주 1~3단계(3222e8a·2169298·f91e9ff) · 정착민 Sunnyside 교체(e4660de) · 골드 소비 건물 3종(b3c9ae4)
- 티어 2: 겨울 위협화(3157d11) · 부상 시스템(67fd621) · GOALS 21개+진행률(67fd621)
- 티어 3: 신앙 Phase 1(제단·자동축적·임계축복, 커밋 73c9629 계열) · deliver 파이프라인 활성화(d31f071)
- 버그: 역할 정착민 다중 작업 지시 시 제자리 진동(543fca2 — 양보 조건에 jobInRole 가드)

</details>

## 진행 원칙 (모든 티어 공통)
1. 항목 착수 전 RECIPES.md 해당 레시피 Read → 체크리스트로 사용.
2. `npm run check:style` → 구현 → `npm run test:sim` → `npm run test:invariants` → (새 기능이면) sim-smoke 시나리오 추가.
3. 세이브 스키마를 바꾸면 `js/save.js` 의 `v` 를 올릴지 판단(필드 추가뿐이면 대부분 불필요 — ARCHITECTURE.md 세이브 섹션 참고).
4. 새 무작위 요소는 반드시 주입 rng 경유 + `createWorld` 소비 순서 불변(파생 시드 사용).
5. 사건·전투 신규 콘텐츠는 소크 불변식(6시드×30일)을 깨지 않는 피해 상한을 config 에 명시.
6. 완료 후 이 문서에 체크 표시 + 커밋 해시 기록, 새로 떠오른 아이디어는 알맞은 티어에 추가.
