# ROADMAP.md — 다음 작업 백로그 (2026-07 기준)

> 새 세션이 이 문서만 읽고 바로 작업을 고를 수 있도록 우선순위·근거·시작점을 적는다.
> 항목을 시작하기 전에 **RECIPES.md** 에서 해당 레시피(건물/적/작업/자원/테스트)를 먼저 읽을 것.
> 완료한 항목은 체크(`[x]`)하고 커밋 해시를 남긴다. 새로 생긴 아이디어는 알맞은 티어에 추가.

---

## 티어 0 — 바로 처리(가벼움, 세션 워밍업용)

- [ ] **최근 콘텐츠 도전과제(GOALS) 반영** — `js/goals.js`(19번째 줄 `GOALS` 배열). 성문·찬란한 음식·초특급 희귀 낚시 스팟이 추가됐는데 도전과제엔 없음. 예: "출입 통제"(성문 1개 완공) · "기적의 만찬"(찬란한 음식 1회 섭취) · "심해의 전설"(spotOnly 어종 1회 포획). `test` 함수는 `world`/`pawns` 상태만 보므로 플래그가 없으면 `world.stock`/`world.buildings` 직접 검사로 판정(예: `countBuilt(w,'fenceGate')>=1`). 완료 시 sim-smoke 시나리오 1개 추가(레시피 5-A).
- [ ] **hook-style.mjs 활성화** — `tools/hook-style.mjs` 는 만들어져 있지만 `.claude/settings.json` 자동 등록이 권한 문제로 막혀 있음(수동 등록 대기 상태, 메모리 `project-capability-kit` 참고). 대장님께 등록 JSON 안내 후 승인받아 PostToolUse 훅으로 연결하면, `js/*.js` 수정 시마다 `check:style` 이 자동 실행되어 이후 세션(특히 하위 모델)의 스타일·결정론 위반을 즉시 차단할 수 있음.
- [ ] **ARCHITECTURE.md 정기 드리프트 점검** — 이번에 `MAP_W/H`(96→128 반영 누락)·원정 섬 개수(3→4)·`isWalkable(forEnemy)` 시그니처 등 stale 사실 일부를 고쳤음. 기능 추가할 때마다 관련 섹션이 실제 코드와 맞는지 한 줄이라도 확인하는 습관화.

## 티어 1 — 레시피 그대로 적용 가능(1세션 완결, 설계 불필요)

- [ ] **연구(RESEARCH) 트리 확장** — `js/config.js:290` `RESEARCH` 가 현재 `farming`/`blacksmith`/`steel` 3개뿐이라 후반 성장 곡선이 얕음(나라 단계까지 가는 동안 연구할 게 금방 바닥남). 후보: 관개(농사 산출 +%), 항해술(뗏목 대신 배 — 이동속도↑), 요새화(방어건물 상한 해금), 수의학(축사/사냥 산출 보정). `RESEARCH_RATE_PER_PAWN`(config.js:289) 기반 포인트 시스템 재사용, 신규 코드는 각 tech 의 `desc`/`cost` + 해금 게이트(`world.research.unlocked.xxx`)뿐이라 설계 부담 적음.
- [ ] **작물·요리 재료 다양화** — 현재 `CROP`/`FRUITTREE`(밀·과일) 2종뿐. 당근·감자 등 신규 작물 추가로 `COOK_TIERS`(config.js:419) 재료 폭을 넓힘. 레시피 4-C 그대로 적용(배열 순서=등급 오름차순 유지 필수).
- [ ] **가축 사육 산출물 다양화** — `ANIMALS`(config.js:427)의 양/돼지/소/닭이 전부 food+leather+meat 패턴만 반복. 예: 양은 양털(신규 자원 `wool`)도 겸용 산출해 가죽과 용도 분리 — 레시피 4-A(스톡 자원 "자동 3+수동 3") 그대로.
- [ ] **유틸리티 건물 보강** — 저장고 특화(부패 방지·보존창고), 정착민 사기 증진 건물(예: 정자·목욕탕 계열, `mood` 회복 보정) 등. 레시피 1 그대로, 신규 `effect.key` 는 `world.upgradeMult`/`upgradeAdd` 패턴 재사용 가능.

## 티어 2 — 설계 먼저, 2~3세션 규모

- [ ] **겨울(SEASONS) 실제 위협화** — `SEASONS`(config.js:591)의 겨울은 현재 화면 색조(tint)+`noFarm` 뿐, 실질적 생존 압박이 없음(Frostpunk 벤치마킹 여지). 방향: 난방 반경 개념(모닥불/전용 난로 근처만 온기) + 겨울철 냉기 데미지(허기처럼 `pawn.cold` 필드) + 겨울 진입 전 대비(장작 비축) 유도. `pawns.js` 욕구 로직(`updatePawn`)과 `world.js` 계절 전환 지점에 훅 필요 — 기존 허기/체력 시스템과 대칭적으로 설계하면 무리 없음.
- [ ] **정착민 부상 시스템** — 현재 `pawn.hp` 만 있고 "부상 상태"(예: 다리 부상→이동속도 저하, 손 부상→작업속도 저하) 개념이 없음. `CLINIC`(config.js:246) 치료소와 자연스럽게 엮임(부상 = 치료소 방문 사유 다양화). `pawn.injury` 필드 + `greedyStep`/`workMult` 계산에 페널티 곱 추가하는 정도로 시작 가능.
- [ ] **도전과제(GOALS) 전면 확장 + 진행률 UI** — 티어 0의 즉시 반영을 넘어, 8개→20개 이상으로 늘리고 ui.js 에 "달성 N/전체" 진행률 표시 추가. 신규 콘텐츠(원정 섬 4곳 전부 발견·보스 처치·연구 트리 올클리어 등)를 도전과제화하면 플레이어 목표감이 크게 개선됨.

## 티어 3 — 대형 설계 필요(리서치→설계 문서→구현, 여러 세션)

- [ ] **아보랑카도 신앙 시스템 심화** — 이전 세션에서 검토만 하고 미착수. 현재 「아보랑카도」는 `GODDESS.day`(7일차) 밤 1회성 강림 후 끝(`js/config.js` GODDESS, `js/sim.js` 강림 로직). 확장 아이디어: 제단 건물 신설 → 정착민이 주기적으로 "기도"(신규 job 타입, 레시피 3 그대로 적용 가능) → 신앙도(favor) 자원처럼 축적 → 임계값마다 자동/선택적 축복(RELICS 재사용). RimWorld ideology, Frostpunk faith 노선 벤치마킹. **먼저 짧은 설계 메모(원하는 루프·리스크)를 만들고 착수 권장** — 세이브 스키마 영향 있으면 `save.js` 버전 업 필요.
- [ ] **정착민 사회/관계 시스템** — 정착민 간 호감도·갈등(다툼·불화로 사기 저하 등). 완전 신규 시스템(관계 매트릭스 자료구조, 이벤트, UI 패널)이라 스코프가 큼. 소규모 파일럿(정착민 쌍마다 호감도 하나만) → 반응 좋으면 확장 추천.
- [ ] **운반(haul) 자동화를 건설까지 확장** — 현재 건설 자재는 착공 시 즉시 재고에서 차감(운반 단계 없음, `ARCHITECTURE.md` 재고 모델 참고). `haul` 작업 타입이 이미 있으니, 건설도 "자재를 실제로 날라야 완공"하도록 바꾸면 They Are Billions 식 물류 최적화 재미가 생김. 다만 `stepWorld` SSOT·예약락(`world.reserved`) 영향 범위가 크고 기존 세이브 호환도 걸림 — **가장 리스크가 큰 항목, 뒤로 미뤄도 됨**.

---

## 진행 원칙 (모든 티어 공통)
1. 항목 착수 전 RECIPES.md 해당 레시피 Read → 체크리스트로 사용.
2. `npm run check:style` → 구현 → `npm run test:sim` → `npm run test:invariants` → (새 기능이면) sim-smoke 시나리오 추가.
3. 세이브 스키마를 바꾸면 `js/save.js` 의 `v` 를 올릴지 판단(필드 추가뿐이면 대부분 불필요 — ARCHITECTURE.md 세이브 섹션 참고).
4. 완료 후 이 문서에 체크 표시 + 커밋 해시 기록, 새로 떠오른 아이디어는 알맞은 티어에 추가.
