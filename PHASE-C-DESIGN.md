# Phase C 설계 제안 — 나라의 시련 (대침공과 승리)

> **상태: 검토 중 — 구현 미착수.** 대장님 확인 후 착수. 착수 전 확인 필요 섹션의 질문에 먼저 답해주세요.
> 대상 단계: 나라(`RANKS[4]`). 선행 시스템: B.1(발전 단계) · B.2(방어 tier·대포) · 유물 시스템 · 원정 섬.

## 왜 이 방향인가

- **발전 단계에 정점이 없음** — 나라 단계는 도달해도 대포 하나 풀리고 끝. "여기까지 오면 뭐가 있지?"에 대한 답이 없다.
- **방어 tier·대포가 놀고 있음** — B.2에서 성 3단계 대포(광역 포격)까지 만들었는데, 정작 평범한 고블린 습격에만 쓰인다. 진짜 무대가 없다.
- **전투가 무한 반복** — 습격은 날짜가 지날수록 그냥 숫자(HP·마릿수)만 커진다. 클라이맥스 없이 계속 같은 패턴.
- **유물을 모으는 이유가 흐릿함** — 시너지는 있지만 "이걸 다 모으면 뭘 하는가"에 대한 목표점이 없다.

네 가지가 전부 **"나라 단계 도달 후 벌어지는 하나의 큰 사건"**으로 자연스럽게 묶인다.

## 진행 순서 (C.1이 C.2를 트리거, C.2 결과가 C.3을 판정하는 실제 의존 순서)

### C.1 — 침공 예고와 준비 기간
나라 단계 도달 시, **10일 후 대침공**이 온다는 예고(토스트 + 상단 카운트다운 뱃지). 방어를 준비할 시간을 명확히 준다 — 성 대포 완성, 정착민 무장, 창고 확충.
발전 패널(🏛️)에 "다음 대침공까지 N일" 표시 추가.

### C.2 — 대침공 (파도식 습격)
예고된 날 밤, 기존 고블린 습격보다 훨씬 강한 **3웨이브** 공격. 웨이브 사이 짧은 소강 시간(회복·재정비 기회). 신규 적 **「정복자」(warlord)** — 식인종보다 강한 미니보스가 각 웨이브에 동반되며, 정복자를 먼저 쓰러뜨리면 그 웨이브가 조기 종료.

| 항목 | 값 |
|---|---|
| 기반 스탯 | GIANT × 1.3 |
| 웨이브 수 | 3 |
| 웨이브 간격 | 단축된 소강 시간 |
| 재사용 패턴 | `enemyStats` kind 분기 |

### C.3 — 승리 판정과 보상
모든 웨이브 격퇴 시 **"콜로니의 승리"** 축하 연출(토스트+이벤트+효과음) + 전설급 유물 확정 획득. `goals.js`에 "대침공 격퇴" 목표 추가.
패배(전멸 위기) 시엔 게임오버 대신 **침공이 물러가고 재도전 카운트다운이 다시 시작**되는 방향을 권장 — 이 게임은 영구사망형이 아니라 지속형 빌더이기 때문.

### C.4 — 그 이후 (선택 범위, 이번엔 제외 권장)
승리 후에도 게임은 이어진다. 두 방향 검토: ① RANKS에 "제국" 단계를 추가해 계속 성장, ② 더 강해진 침공이 주기적으로 재도전 형태로 반복. **이번 phase 범위엔 포함하지 않는 것을 권장** — C.1~C.3을 먼저 안정화한 뒤, 반응을 보고 별도 phase로 분리.

## 파일 영향 범위 (예상)

| 파일 | 역할 | 변경 내용 |
|---|---|---|
| `config.js` | 설정 | WARLORD 스탯, INVASION 스케줄, 전설급 RELICS 소수 |
| `world.js` | 로직 | spawnRaid 확장 재사용한 웨이브 스폰, `world.invasion` 상태 |
| `sim.js` | SSOT | 나라 도달 감지→카운트다운 시작, 웨이브 트리거·격퇴 판정 |
| `goals.js` | 목표 | "대침공 격퇴" 신규 목표 |
| `ui.js` / `main.js` | UI | 카운트다운 뱃지, 승리 모달 |
| `render.js` | 렌더 | 정복자 스프라이트(기존 틴트 재사용), 승리 이펙트 |

B.2에서 확인된 대로 `enemyStats(e)` kind 분기·`spawnRaid(world,count,rng,kind)` 재사용 패턴을 그대로 따른다 — 새 훅 최소화.

## 검토했지만 이번엔 제외한 대안

- **항해·교역 시스템** — 뗏목 이동 자체를 개선하는 방향도 고려했지만, 이미 2번대륙 이동에서 검증된 기존 메커니즘이라 급한 문제는 아니라고 판단.
- **정착민 심화(다운·멘탈붕괴)** — 이전에 롤백된 방향이라 이번 제안엔 넣지 않음.
- **섬 추가 확장** — "더 많은 섬"은 콘텐츠 양은 늘지만 새로운 긴장감을 만들진 않아, 정점 이벤트보다 우선순위를 낮춤.

## 착수 전 확인 필요 (대장님 결정 사항)

1. 패배 시 "재도전 카운트다운"이 아니라 실제 페널티(자원 손실 등)를 원하시는지?
2. 승리 연출의 무게감 — 가벼운 축하 토스트 정도면 되는지, 별도 전용 모달까지 원하시는지?
3. C.4(그 이후)를 이번 범위에 넣을지, 반응 보고 나중에 별도로 할지?

---
*이 문서는 검토용 아티팩트(https://claude.ai/code/artifact/8cdae4bd-8a09-427c-a3f2-e9a9c881f9bc)로도 볼 수 있음(시각적 버전, 동일 내용).*
